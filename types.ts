// Date is a type so you can implement methods on it
const today = new Date();
today.getMonth();

// this is an object so you
const person = {
	age: 20
};

class Color {
	//
}
const red = new Color();
